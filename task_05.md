# Task 05: Append hello to text.txt

## Goal

- Append the string 'hello' to `./text.txt`.

## Steps

1. Append 'hello' followed by a newline to `./text.txt`.
2. Verify that `./text.txt` has been updated correctly.

## Result

The task has been successfully completed.

1. **Appended 'hello\n' to `./text.txt`**: Since `./text.txt` did not exist
   initially, the file was created and the string `'hello\n'` was written to it.
2. **Verification**: The content of `./text.txt` was read back and verified to
   be:
   ```
   hello
   ```
