# 手動実機テストシナリオ：バックグラウンドループの並行動作と自律停止（YOLOモード）

このシナリオは、修正された `loopScheduler.ts`
の自己停止ガードが、人間との対話的なCLI利用シーンにおいて正しく、安全かつ完全に動作することを確認するための実機テスト手順書です。

---

## 【事前準備】

1.  **古い状態のクリア**: ワークスペース（プロジェクトのルート）に `text.txt`
    が存在しないことを確認します。存在する場合は削除してください。

    ```bash
    rm -f text.txt
    ```

2.  **YOLOモードでのCLI対話起動**: ツール実行の確認プロンプト（ASK_USER）による実行ハングを防ぐため、常に自動承認される
    **YOLOモード** で対話型CLIを起動します。
    ```bash
    npm run start -- --approval-mode=yolo
    ```
    _(※ Inkによる対話画面がターミナル上で起動します。)_

---

## 【テスト手順】

### ステップ 1: バックグラウンドループの登録（非ブロッキングの検証）

1.  対話画面のメッセージ入力欄で、以下の `/loop` コマンドを入力して実行します。
    ```text
    /loop --interval 15s --background "Check if ./text.txt exists in the workspace. If the file exists, use the loop-stop tool to stop this loop."
    ```
2.  **期待される結果:** 画面に
    `Background loop has been scheduled successfully.`
    と表示され、15秒間隔の次の実行予定時刻が表示されます。

3.  **並行会話のテスト (重要):**
    ループが登録されたら、**15秒経過するのを待たずに**、すぐにチャット入力欄で通常の質問を送信してみてください。
    _入力例:_ `日本の首都はどこですか？簡単な3単語で教えて。`
4.  **期待される結果:**
    裏でタイマーが稼働している間も、表のチャットプロセスは何の影響（ブロックやミリ秒の遅延）も受けず、即座に「東京です」とLLMからの回答を返してきます。

---

### ステップ 2: バックグラウンド実行の目視観測

1.  15秒が経過するのを待ちます。
2.  **期待される結果:**
    タイマー発火と同時に、画面上にバックグラウンドプロセスが起動したことが（`stdio: 'inherit'`
    を通して）表示され、エージェントがワークスペース上の `text.txt`
    を調査し始める挙動が観測されます。
3.  **エージェントの判定:** 現時点では `text.txt`
    は存在しないため、バックグラウンドエージェントは「ファイルが見つかりません。ループ停止コマンドは実行しません」という旨の出力を残して子プロセスを続行します。
4.  **状態確認:** メインチャット欄で `/loop status` コマンドを実行します。
    _期待される結果:_ `Loop is scheduled to run next at [15秒後の時刻].`
    と表示され、次のループ実行予定時間が進んでいることを確認します。

---

### ステップ 3: トリガーファイルの作成と自律停止（Self-Stop）の検証

1.  不定期なタイミングで `text.txt`
    別のターミナルを開いて直接ファイルを作成します。
2.  `text.txt`
    が作成されたら、次のバックグラウンドタイマー（最大15秒）が発火するのを待ちます。
3.  **期待される結果 (自律停止の瞬間):**
    - 15秒後にバックグラウンド子プロセスが起動。
    - エージェントが `text.txt` の存在を検知。
    - エージェントがプロンプトの指示に基づき、自律的に `loop-stop`
      ツールを実行。
    - 画面上に `Background loop stopped.` という終了メッセージが表示されます。
4.  **最終ステータスの確認:** メインチャット欄で `/loop status`
    コマンドを実行します。
5.  **期待される結果:** `No loop is currently scheduled.`
    (現在スケジュールされているループはありません) と表示され、子プロセスが終了した際に**親プロセス側のタイマーも安全かつ完全に終了（停止）されたこと**を確認します。

---

## 【後片付け】

1.  テストで使用した `text.txt` を削除して終了します。
    ```bash
    rm -f text.txt
    ```
